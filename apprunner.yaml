version: 1.0
runtime: nodejs16
build:
  commands:
    pre-build:
      - npm ci
      - npm run build
    build:
      - npm ci

run:
  command: npm serve
  secrets:
    - name: doppler
      value-from: "arn:aws:secretsmanager:us-east-1:792017225318:secret:doppler-q8uhG9"